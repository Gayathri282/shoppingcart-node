<section>
    <div class="container">
        <div class="row mt-3">
            <a href="/admin/addProduct" class="btn btn-success m-auto w-50">Add Product</a>
        </div>
        <table class="table mt-4">
  <thead>
    <tr>
      <th scope="col">Id</th>
      <th scope="col">Name</th>
      <th scope="col">Email</th>
      {{!-- <th scope="col">Category</th>
      <th scope="col">Description</th>
      <th scope="col">Price</th> --}}
    </tr>
  </thead>
  <tbody>
            {{#each users}}
    <tr>
      {{!-- <th scope="row">1</th>
      <td>{{this.name}}</td> --}}
      {{!-- <td><img src="/productImages/{{this._id}}.jpg" alt=""style="height:100px;width:100px;"></td> --}}
      <td>{{this._id}}</td>
      <td>{{this.name}}</td>
      <td>{{this.email}}</td>
      {{!-- <td><a href="/admin/editProduct/{{this._id}}" class="btn btn-primary">Edit</a></td> --}}
      <td><a href="/admin/deleteUser?id={{this._id}}" class="btn btn-danger" onclick="return confirm('Do you want to delete {{this.name}}')">X</a></td>
    </tr>
          {{/each}}
  </tbody>
</table>
    </div>
</section>

<style>
  @media(max-width:786px){
    section{
      font-size: 8px;
      
    }
  }
</style>